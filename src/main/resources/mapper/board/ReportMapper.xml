<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.back.board.report.mapper.ReportMapper">

    <!-- 신고 사유 카테고리 목록 (reasons 테이블 노출 — FE 하드코딩 제거용) -->
    <!-- 활성 사유(is_active=1)만, 노출 순서 오름차순 -->
    <select id="findReasons" resultType="com.back.board.report.dto.ReportReasonResponse">
        SELECT reason_id     AS reasonId,
               code,
               label,
               display_order AS displayOrder
        FROM reasons
        WHERE is_active = 1
        ORDER BY display_order ASC
    </select>

    <!-- 사유 존재/활성 검증 + detail 정책(code=ETC 일 때만 상세 입력) 판정용.
         reason_id 는 reports_log 의 FK 라, 검증 없이 넣으면 잘못된 값이 제약 위반 500 으로 터진다. -->
    <select id="findActiveReasonCode" resultType="string">
        SELECT code
        FROM reasons
        WHERE reason_id = #{reasonId}
          AND is_active = 1
    </select>

    <insert id="insertReport">
        INSERT INTO reports_log (reporter_id, target_type, target_id, reason_id, detail, status, created_at)
        VALUES (#{reporterId}, #{targetType}, #{targetId}, #{reasonId}, #{detail}, 'pending', NOW())
    </insert>

    <!-- 신고 대상은 게시판 종류에 상관없이 posts/comments 테이블을 공용으로 사용하므로 직접 조회 -->
    <select id="findPostAuthorId" resultType="long">
        SELECT user_id FROM posts WHERE post_id = #{postId}
    </select>

    <select id="findCommentAuthorId" resultType="long">
        SELECT user_id FROM comments WHERE comment_id = #{commentId}
    </select>

    <select id="findPostState" resultType="string">
        SELECT state FROM posts WHERE post_id = #{postId}
    </select>

    <select id="findCommentState" resultType="string">
        SELECT state FROM comments WHERE comment_id = #{commentId}
    </select>

</mapper>
