<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.back.board.attachment.mapper.AttachmentMapper">

    <!-- 선업로드: post_id 는 NULL (글이 아직 없다). uploader_id 가 소유자 판정 근거가 된다 -->
    <insert id="insertPending" useGeneratedKeys="true" keyProperty="attachmentId">
        INSERT INTO attachments (post_id, uploader_id, file_name, file_url, file_type, usage_type, file_size, created_at)
        VALUES (NULL, #{uploaderId}, #{fileName}, #{fileUrl}, #{fileType}, #{usageType}, #{fileSize}, NOW())
    </insert>

    <!-- 파일 조회 권한 판정용 1행.
         경로(/api/user/files/{fileId})에 boardId 가 없으므로 글을 타고 게시판까지 조인해
         read_scope 를 판정할 재료를 함께 가져온다. 선업로드 파일은 postState/boardId 가 null 이다 -->
    <select id="findFileRow" resultType="com.back.board.attachment.dto.AttachmentFileRow">
        SELECT a.attachment_id AS attachmentId,
               a.post_id       AS postId,
               a.uploader_id   AS uploaderId,
               a.file_url      AS fileUrl,
               a.file_name     AS fileName,
               a.file_type     AS fileType,
               a.usage_type    AS usageType,
               a.file_size     AS fileSize,
               a.state         AS state,
               p.state         AS postState,
               p.board_id      AS boardId
        FROM attachments a
        LEFT JOIN posts p ON a.post_id = p.post_id
        WHERE a.attachment_id = #{fileId}
    </select>

    <!-- 선업로드 파일 → 글 연결. 내 것이고 아직 미연결인 행만 바뀐다 -->
    <update id="linkToPost">
        UPDATE attachments
        SET post_id = #{postId}
        WHERE post_id IS NULL
          AND uploader_id = #{uploaderId}
          AND state = 'normal'
          AND attachment_id IN
          <foreach collection="attachmentIds" item="id" open="(" separator="," close=")">
              #{id}
          </foreach>
    </update>

    <!-- 수정 후 본문에서 사라진 인라인 이미지 (첨부 목록용 파일은 deleteAttachmentIds 로만 지운다) -->
    <select id="findUnreferencedInline" resultType="com.back.board.attachment.dto.PendingAttachmentRow">
        SELECT attachment_id AS attachmentId,
               file_url      AS fileUrl
        FROM attachments
        WHERE post_id = #{postId}
          AND usage_type = 'inline'
          AND state = 'normal'
        <if test="keepIds != null and keepIds.size() > 0">
          AND attachment_id NOT IN
          <foreach collection="keepIds" item="id" open="(" separator="," close=")">
              #{id}
          </foreach>
        </if>
    </select>

    <update id="softDeleteByIds">
        UPDATE attachments
        SET state = 'deleted'
        WHERE state = 'normal'
          AND attachment_id IN
          <foreach collection="attachmentIds" item="id" open="(" separator="," close=")">
              #{id}
          </foreach>
    </update>

    <!-- 보존기간이 지난 미연결 파일. 글에 붙은 파일(post_id NOT NULL)은 절대 대상이 아니다 -->
    <select id="findExpiredPending" resultType="com.back.board.attachment.dto.PendingAttachmentRow">
        SELECT attachment_id AS attachmentId,
               file_url      AS fileUrl
        FROM attachments
        WHERE post_id IS NULL
          AND created_at &lt; DATE_SUB(NOW(), INTERVAL #{hours} HOUR)
    </select>

    <!-- 물리 삭제 — 글에 속한 적이 없는 파일이라 소프트삭제 대전제의 예외(초안과 같은 논리).
         post_id IS NULL 조건을 다시 걸어, 그 사이 발행된 파일이 지워지는 것을 막는다 -->
    <delete id="deletePendingByIds">
        DELETE FROM attachments
        WHERE post_id IS NULL
          AND attachment_id IN
          <foreach collection="attachmentIds" item="id" open="(" separator="," close=")">
              #{id}
          </foreach>
    </delete>

    <select id="findPolicySetting" resultType="string">
        SELECT setting_value FROM policy_settings WHERE code = #{code}
    </select>
</mapper>
