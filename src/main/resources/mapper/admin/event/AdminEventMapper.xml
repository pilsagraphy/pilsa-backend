<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.back.admin.event.mapper.AdminEventMapper">

    <insert id="insertEvent" useGeneratedKeys="true" keyProperty="request.eventId">
        INSERT INTO events (
            title,
            category,
            description,
            start_at,
            end_at,
            user_id,
            created_at,
            updated_at
        ) VALUES (
                     #{request.title},
                     #{request.category},
                     #{request.description},
                     <!-- 시각이 오면 붙이고, 없으면 00:00:00 (종일). 서비스가 'HH:mm' 을 검증해 넘긴다 -->
                     CONCAT(#{request.startDate}, ' ', COALESCE(#{request.startTime}, '00:00'), ':00'),
                     CONCAT(#{request.endDate},   ' ', COALESCE(#{request.endTime},   '00:00'), ':00'),
                     #{userId},
                     NOW(),
                     NOW()
                 )
    </insert>

    <update id="updateEvent">
        UPDATE events
        <set>
            <if test="request.title != null">title = #{request.title},</if>
            <if test="request.category != null">category = #{request.category},</if>
            <if test="request.description != null">description = #{request.description},</if>
            <if test="request.startDate != null">
                start_at = CONCAT(#{request.startDate}, ' ', COALESCE(#{request.startTime}, '00:00'), ':00'),
            </if>
            <if test="request.endDate != null">
                end_at = CONCAT(#{request.endDate}, ' ', COALESCE(#{request.endTime}, '00:00'), ':00'),
            </if>
            updated_at = NOW()
        </set>
        WHERE event_id = #{eventId} AND state = 'normal'
    </update>

    <!-- 소프트 삭제 (프로젝트 전역 원칙: 물리 삭제 없음) -->
    <update id="deleteEvent">
        UPDATE events SET state = 'deleted', updated_at = NOW()
        WHERE event_id = #{eventId} AND state = 'normal'
    </update>

    <!-- 카테고리 정본 이름 조회 (event_categories 가 events.category 값의 정본).
         콜레이션(utf8mb4_0900_ai_ci)이 대소문자를 무시하므로 'mt' 로 찾아도 'MT' 가 나온다 —
         그 반환값을 저장해야 events.category 의 표기가 갈리지 않는다. 없으면 null → 400. -->
    <select id="findActiveCategoryName" resultType="string">
        SELECT name
        FROM event_categories
        WHERE name = #{name} AND is_active = 1
    </select>

    <!-- 수정 시 존재 확인. 카테고리 400 보다 404 가 먼저 나가야 하므로 UPDATE 앞에서 판정한다 -->
    <select id="existsEvent" resultType="boolean">
        SELECT EXISTS (
            SELECT 1 FROM events
            WHERE event_id = #{eventId} AND state = 'normal'
        )
    </select>

    <!-- ───── 일정 이미지(첨부) ───── -->
    <insert id="insertImage" useGeneratedKeys="true" keyProperty="row.imageId">
        INSERT INTO event_images (event_id, file_url, file_name, file_type, file_size, sort_order)
        VALUES (#{row.eventId}, #{row.fileUrl}, #{row.fileName}, #{row.fileType}, #{row.fileSize}, #{sortOrder})
    </insert>

    <select id="countImages" resultType="int">
        SELECT COUNT(*) FROM event_images
        WHERE event_id = #{eventId} AND state = 'normal'
    </select>

    <select id="findImage" resultType="com.back.event.dto.EventImageRow">
        SELECT image_id AS imageId, event_id AS eventId, file_url AS fileUrl, file_name AS fileName,
               file_type AS fileType, file_size AS fileSize, state
        FROM event_images
        WHERE image_id = #{imageId} AND event_id = #{eventId} AND state = 'normal'
    </select>

    <!-- 물리 삭제 대신 소프트 삭제 — 파일은 서비스가 커밋 뒤에 지운다 -->
    <update id="softDeleteImage">
        UPDATE event_images SET state = 'deleted'
        WHERE image_id = #{imageId} AND state = 'normal'
    </update>

</mapper>
