<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.back.admin.sanction.mapper.ReportAdminMapper">

    <!-- ORDER BY 조각 (최신순 기본) -->
    <sql id="orderBy">
        ORDER BY
        <choose>
            <when test="sort == 'oldest'">t.first_reported_at ASC</when>
            <otherwise>t.first_reported_at DESC</otherwise>
        </choose>
    </sql>

    <!-- ===================== 게시글 신고 ===================== -->
    <!-- 대상별로 그룹핑: 최초 신고일시/누적건수/대표(최신)신고 -->
    <sql id="reportedPostsFrom">
        FROM (
            SELECT target_id,
                   MIN(created_at) AS first_reported_at,
                   COUNT(*)        AS report_count,
                   MAX(report_id)  AS last_report_id
            FROM reports_log
            WHERE target_type = 'post'
            GROUP BY target_id
        ) t
        JOIN reports_log r ON r.report_id = t.last_report_id
        JOIN posts   p  ON p.post_id = t.target_id
        JOIN boards  b  ON p.board_id = b.board_id
        JOIN users   u  ON p.user_id = u.user_id
        JOIN reasons rs ON r.reason_id = rs.reason_id
        <where>
            <!-- 상태 필터: state 지정 시 해당 표시상태(blind/deleted)만.
                 미지정 시 기본은 대표 신고가 반려(rejected)된 것만 제외 → pending·blind·deleted(=복구 안 된 것) 노출.
                 (반려=복구된 대상은 status=rejected 로만 구분된다. 표시상태 normal 로 제외하면 미조치 pending 신고까지 사라지므로 status 기준으로 판정) -->
            <choose>
                <when test="state != null and state != ''">AND p.state = #{state}</when>
                <otherwise>AND r.status &lt;&gt; 'rejected'</otherwise>
            </choose>
            <if test="boardId != null">AND p.board_id = #{boardId}</if>
            <if test="keyword != null and keyword != ''">
                AND (p.content LIKE CONCAT('%', #{keyword}, '%') ESCAPE '\\'
                     OR u.name LIKE CONCAT('%', #{keyword}, '%') ESCAPE '\\')
            </if>
        </where>
    </sql>

    <select id="findReportedPosts" resultType="com.back.admin.sanction.dto.ReportedItemResponse">
        SELECT
            'post'                       AS targetType,
            t.target_id                  AS targetId,
            p.post_id                    AS postId,
            LEFT(p.content, 30)          AS preview,
            p.board_id                   AS boardId,
            b.name                       AS boardName,
            u.name                       AS authorName,
            rs.label                     AS reasonLabel,
            t.first_reported_at          AS firstReportedAt,
            t.report_count               AS reportCount,
            r.status                     AS reportStatus,
            p.state                      AS state
        <include refid="reportedPostsFrom"/>
        <include refid="orderBy"/>
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countReportedPosts" resultType="int">
        SELECT COUNT(*)
        <include refid="reportedPostsFrom"/>
    </select>

    <!-- ===================== 댓글 신고 ===================== -->
    <sql id="reportedCommentsFrom">
        FROM (
            SELECT target_id,
                   MIN(created_at) AS first_reported_at,
                   COUNT(*)        AS report_count,
                   MAX(report_id)  AS last_report_id
            FROM reports_log
            WHERE target_type = 'comment'
            GROUP BY target_id
        ) t
        JOIN reports_log r ON r.report_id = t.last_report_id
        JOIN comments c ON c.comment_id = t.target_id
        JOIN posts    p ON c.post_id = p.post_id
        JOIN boards   b ON p.board_id = b.board_id
        JOIN users    u ON c.user_id = u.user_id
        JOIN reasons  rs ON r.reason_id = rs.reason_id
        <where>
            <!-- 상태 필터: state 지정 시 해당 표시상태(blind/deleted)만.
                 미지정 시 기본은 대표 신고가 반려(rejected)된 것만 제외 → pending·blind·deleted(=복구 안 된 것) 노출.
                 (반려=복구된 대상은 status=rejected 로만 구분된다. 표시상태 normal 로 제외하면 미조치 pending 신고까지 사라지므로 status 기준으로 판정) -->
            <choose>
                <when test="state != null and state != ''">AND c.state = #{state}</when>
                <otherwise>AND r.status &lt;&gt; 'rejected'</otherwise>
            </choose>
            <if test="boardId != null">AND p.board_id = #{boardId}</if>
            <if test="keyword != null and keyword != ''">
                AND (c.content LIKE CONCAT('%', #{keyword}, '%') ESCAPE '\\'
                     OR u.name LIKE CONCAT('%', #{keyword}, '%') ESCAPE '\\')
            </if>
        </where>
    </sql>

    <select id="findReportedComments" resultType="com.back.admin.sanction.dto.ReportedItemResponse">
        SELECT
            'comment'                    AS targetType,
            t.target_id                  AS targetId,
            p.post_id                    AS postId,
            LEFT(c.content, 30)          AS preview,
            p.board_id                   AS boardId,
            b.name                       AS boardName,
            u.name                       AS authorName,
            rs.label                     AS reasonLabel,
            t.first_reported_at          AS firstReportedAt,
            t.report_count               AS reportCount,
            r.status                     AS reportStatus,
            c.state                      AS state
        <include refid="reportedCommentsFrom"/>
        <include refid="orderBy"/>
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countReportedComments" resultType="int">
        SELECT COUNT(*)
        <include refid="reportedCommentsFrom"/>
    </select>

    <!-- ===================== 대상 기준 개별 신고 목록 (신고 처리 모달 '신고자 목록') ===================== -->
    <!-- 신고자 id/이름은 select 하지 않는다 — 응답에 실리면 화면상 익명이어도 개발자도구로 노출되므로.
         created_at 오름차순: 프론트가 이 순서대로 익명A·익명B… 를 붙인다.
         detail 은 신고자가 적은 상세(기타 사유일 때만 값, 그 외 null). post/comment 공용(target_type 파라미터로 구분). -->
    <select id="findReportsByTarget" resultType="com.back.admin.sanction.dto.ReportEntryResponse">
        SELECT r.report_id   AS reportId,
               rs.label      AS reasonLabel,
               r.detail      AS detail,
               r.created_at  AS createdAt
        FROM reports_log r
        LEFT JOIN reasons rs ON r.reason_id = rs.reason_id
        WHERE r.target_type = #{targetType}
          AND r.target_id = #{targetId}
        ORDER BY r.created_at ASC, r.report_id ASC
    </select>

    <!-- ===================== 조치 보조 ===================== -->
    <select id="findLatestReasonId" resultType="long">
        SELECT reason_id
        FROM reports_log
        WHERE target_type = #{targetType}
          AND target_id = #{targetId}
        ORDER BY report_id DESC
        LIMIT 1
    </select>

    <!-- 처리 시각과(수락이면) 유발 조치를 함께 기록 - 제재회원 현황의 '신고 삭제처리 건수' 집계 근거 -->
    <update id="updatePendingReportsStatus">
        UPDATE reports_log
        SET status = #{status},
            resolved_at = NOW(),
            resolution_action_id = #{resolutionActionId}
        WHERE target_type = #{targetType}
          AND target_id = #{targetId}
          AND status = 'pending'
    </update>

</mapper>
