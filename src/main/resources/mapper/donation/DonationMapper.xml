<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.back.donation.mapper.DonationMapper">

    <select id="selectDonationList" resultType="com.back.donation.dto.DonationResponse">
        SELECT
            donation_id AS donationId,
            amount,
            is_anonymous AS isAnonymous,
            display_name AS displayName,
            affiliation,
            major,
            message,
            donated_at AS donatedAt,
            photo_url AS photoUrl
        FROM
            donations
        ORDER BY
            amount DESC,
            donated_at DESC
            LIMIT 14
    </select>

</mapper>