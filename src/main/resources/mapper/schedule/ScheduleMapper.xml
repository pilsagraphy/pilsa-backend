<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.back.schedule.mapper.ScheduleMapper">

    <insert id="insertSchedule" useGeneratedKeys="true" keyProperty="request.scheduleId">
        INSERT INTO events (
            title,
            description,
            start_at,
            end_at,
            user_id,
            created_at,
            updated_at
        ) VALUES (
                     #{request.title},
                     #{request.title},
                     #{request.startDate},
                     #{request.endDate},
                     #{userId},
                     NOW(),
                     NOW()
                 )
    </insert>

    <update id="updateSchedule">
        UPDATE events
        <set>
            <if test="request.title != null">title = #{request.title},</if>
            <if test="request.startDate != null">start_at = #{request.startDate},</if>
            <if test="request.endDate != null">end_at = #{request.endDate},</if>
            updated_at = NOW()
        </set>
        WHERE event_id = #{scheduleId}
    </update>

    <delete id="deleteSchedule">
        DELETE FROM events
        WHERE event_id = #{scheduleId}
    </delete>

    <select id="findSchedulesByPeriod" resultType="com.back.schedule.dto.ScheduleDataResponse">
        SELECT
            event_id AS scheduleId,
            title,
            DATE_FORMAT(start_at, '%Y-%m-%d') AS startDate,
            DATE_FORMAT(end_at, '%Y-%m-%d') AS endDate
        FROM events
        WHERE start_at &lt;= #{to}
          AND end_at &gt;= #{from}
        ORDER BY start_at ASC
    </select>

</mapper>