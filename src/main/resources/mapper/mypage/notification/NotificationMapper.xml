<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.back.mypage.notification.mapper.NotificationMapper">

    <!--
      알림함 화면(목록·읽음·삭제)에 필요한 쿼리는 아직 없다 — 담당자 과제.
      과제 설명: docs/integration-20260814/HANDOFF-notification-tasks.md
      (notifications 테이블과 발행은 정상 동작하므로 데이터는 계속 쌓인다)
    -->

    <insert id="insertNotification">
        INSERT INTO notifications (user_id, type, title, message, link_url, target_type, target_id, created_at)
        VALUES (#{userId}, #{type}, #{title}, #{message}, #{linkUrl}, #{targetType}, #{targetId}, NOW())
    </insert>

    <!-- 탈퇴 시 본인 알림 일괄 소프트삭제 -->
    <update id="softDeleteAllByUser">
        UPDATE notifications
        SET state = 'deleted'
        WHERE user_id = #{userId}
          AND state = 'normal'
    </update>

</mapper>
